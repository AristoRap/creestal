# TODO

## Coming Up

1. Improve scaffold quality and styling so `creestal new` generates a beautiful base site.
2. Keep Bootstrap as the current default for now, then evaluate a `new` command flag for scaffold flavor selection (for example Tailwind, DaisyUI CDN, etc.) to speed up setup without tightly coupling the generator to one framework.
3. Explore custom generator templates via a `--template` override on `creestal new` so scaffold templates can be customized.
